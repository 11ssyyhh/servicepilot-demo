# 初赛提交检查清单

| 项目 | 要求 | 状态 |
|---|---|---|
| 作品简介 | 500 字内，覆盖问题、方案、创新、开放价值、当前进展 | 已完成（docs/01） |
| 方案 PPT/PDF | 场景价值、方案设计、Skill 与工具集成、可行性与落地计划 | 已完成（初赛作品说明与 PPT） |
| Agent 数量 | 至少 3 个不同职能 Agent | 7 个 |
| AgentTeams 映射 | 角色编排、任务拆解、上下文传递、协同执行、状态追踪 | 已实现（可运行 demo + docs/08） |
| Skill 清单 | 至少 9 个 Skill，含输入输出、失败处理、安全边界、复用价值 | 17 个，9 个完整规格 |
| 高风险审批/回滚/审计 | L0/L1/L2 分级、幂等、回滚点、审计事件 | 已实现（退款审批通过/拒绝双路径） |
| MCP 或等价契约 | 5 个工具 Schema + 迁移成本 | 7 个工具契约 + Mock Server |
| RAG/上下文 | 知识库 RAG、Agent 记忆、共享状态、轨迹可观测 | 已实现（n-gram 相关性 + 证据引用） |
| 可观测 | Trace/Log/Metrics + 评估回放 | 已实现（output/ + replay.py + evaluate.py） |
| 开源与合规 | 开源范围、协议、依赖、数据授权、维护计划 | 已完成（MIT LICENSE + docs/06） |
| 可验证证据 | Mock 工单、Trace、Metrics 样例 | 已实现（output/ + docs/evidence/） |
| 可运行验证 | demo 闭环 + 测试 | 5 场景闭环 + 26 项测试通过 + CI |

## 关键运行证据

```text
python main.py 1            # 5 个场景闭环，生成 output/ 证据
python evaluate.py          # 6/6 Golden Case 通过，评估报告
python replay.py            # 全链路回放
python approval_server.py   # 人工审批 Web 界面
python mcp_mock_server.py   # MCP 等价 Mock Server
python -m unittest discover -s tests -t . -v  # 26 项测试
```
