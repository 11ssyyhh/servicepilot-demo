services:
  mcp-mock:
    build: .
    command: python mcp_mock_server.py 8001
    ports:
      - "8001:8001"

  servicepilot:
    build: .
    command: python main.py 1
    depends_on:
      - mcp-mock
